// realloc() = This function is used to re allocate the memory to same pointer .
// reallocat (increase or decrease) memory using the same pointer & size .
// ptr = realloc(ptr,newSize) .

#include<stdio.h>
#include<stdlib.h>

int main()
{
    
    
return 0;
}